#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

branch=$(git branch --show-current)

message_file=$1
message=$(cat message_file)

if[[ $message === "" ]]; then
  perfix="[${branch}] "
else
  perfix=""
fi
echo "${perfix}${message}" > $message_file
